Some convenience scrips here.

To execute integration tests, please run:
./run_integration_tests.sh
(docker is expected to be installed)

To run the the application (will run tests + assembly):
./run.sh